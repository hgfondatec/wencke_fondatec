version: 2

seeds:
  - name: raw_beleg_arten
    config:
      tags: ['belege_position']

  - name: raw_akademie_artikel
    config:
      column_types:
        akademie_artikelnummer: varchar
        akademie_hauptlieferantnummer: varchar

  - name: raw_vertreter
    config:
      tags: ['belege_position']

  - name: raw_beleg_gruppe
    